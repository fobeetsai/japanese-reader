# MOJi 個人學習工作台（v13）

進入 `flashcard.html` →「📖 MOJi 學習工作台」。

## 能做什麼

- 開啟 MOJi 官方查詞、收藏、詞表總入口 https://www.mojidict.com/share-center 。會員功能仍由 MOJi 的登入狀態與權限決定。
- 讀取自己匯出的 PDF、UTF-8 TXT、TSV，或貼上自己整理的文字。
- 支援單字、文型與混合牌組。每筆日文、讀音、中文及例句／接續可逐筆修改、取消選取。
- 匯入前可略過四欄完全相同的既有卡片；不覆蓋原牌組或學習紀錄。
- 在牌組與卡片筆記保留 MOJi 分享來源，使用既有抽認卡複習及 GitHub 同步。

## PDF 詞表流程

MOJi 官方說明列有「收藏 → 單詞導出」與詞表內的匯出入口。選取需要的單字、讀音、釋義、例句等欄位，使用你帳號可用的匯出功能。

在工作台選取檔案後，PDF 文字會放進整理區。**PDF 的座標順序不等於語意配對，尤其多欄表格、換行及頁首頁尾。** 請對照原檔，整理為每行四欄，沒有內容仍保留欄位分隔符：

```text
日文 | 讀音 | 中文 | 例句或接續說明
```

以上是格式說明；實際匯入時不要保留標題列。可以選用 Tab 分隔。內容有半形 `|` 時改用 Tab。

一次最多 500 筆；PDF 最多 50 頁、20 MB，需有文字層。掃描 PDF 不會在本功能 OCR。所有解析都在瀏覽器執行，沒有檔案上傳、沒有 AI API 呼叫。首次使用 PDF 解析需下載網站上的 PDF.js 資源，已讀取的資源可依網站快取使用。

## 不是全功能會員串接

截至本次查核，未找到 MOJi 公開提供會員第三方整合的 API 文件。本專案沒有取得 MOJi 的 API 授權，也不擷取 Cookie、會員 Token 或批次鏡像資料庫。詞表總入口不是某份詞表的資料匯出連結；貼上分享網址只保存來源，不會下載整份詞表。

舊版內部 Parse API 試接已改成官方查詞入口。本機 `/api/moji/search` 回傳 410；一般字典補全仍保留，但不再標成 MOJi 官方或會員內容。公開 GitHub Pages 不執行 Python 後端。

若要讓會員詞表、收藏與進度雙向自動同步，下一步需向 MOJi 確認可用 API、OAuth、資料匯出格式及教學再利用條件。會員資格本身不能當成對學生公開轉載素材的授權。僅匯入有權使用的個人內容；GitHub 同步會包含匯入後的牌組。

## 已驗證與待驗證

- `node tests/moji.cjs`：欄位完整性、重複資料、錯誤網址、PDF 文字排序與移除內部 API 呼叫。
- `node tests/sync.cjs`：既有跨裝置同步回歸測試。
- 真實瀏覽器已驗證示例預覽、建卡成功及新增 MOJi 分類。
- 已用自編日中 PDF 驗證隨附 PDF.js 6.3.289 的文字擷取、兩張卡片配對與頁面渲染。
- 尚未拿到使用者實際 MOJi 詞表匯出檔；不能保證所有 PDF 版型自動配對。本次瀏覽器自動選檔受限，該步驟尚未完成端到端測試。

參考：MOJi 官方功能說明 https://www.mojidict.com/support/ （單詞導出、詞表、會員功能）。

PDF.js 6.3.289 取自 npm 官方套件 `pdfjs-dist`，Apache-2.0 授權隨附於 `static/flashcards/vendor/pdfjs/LICENSE`；CMap 授權隨檔保留。
